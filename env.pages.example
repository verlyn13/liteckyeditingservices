# Copy to .env.pages (preferred) or env.pages and fill values. This file is safe to keep in repo.

# Cloudflare Pages (secrets). One KEY=VALUE per line.
# These are loaded by `scripts/deploy/configure-pages-env.sh` and piped to
# `wrangler pages secret put` (encrypted at rest in Cloudflare).

# Turnstile secret (server-side verification)
TURNSTILE_SECRET_KEY=

# SendGrid API key (use test key for Preview if desired)
SENDGRID_API_KEY=

# Optional: Use test Turnstile secret for Preview
# TURNSTILE_TEST_SECRET_KEY=

# Optional: Any additional secrets you wish to store in Pages
# MY_OTHER_SECRET=

