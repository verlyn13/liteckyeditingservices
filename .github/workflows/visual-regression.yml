name: Visual Regression (legacy - manual only)

# This workflow is deprecated in favor of e2e-visual.yml
# Kept for manual dispatch only for migration period
# TODO: Remove after confirming e2e-visual.yml covers all use cases

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation notice
        run: |
          echo "⚠️  This workflow is deprecated."
          echo ""
          echo "Visual regression tests are now handled by:"
          echo "  • e2e-visual.yml (PR and main branch)"
          echo "  • visual-modern.yml (baseline updates)"
          echo ""
          echo "This legacy workflow assumed the old Playwright snapshot path:"
          echo "  tests/e2e/visual.spec.ts-snapshots/"
          echo ""
          echo "Current snapshot location (managed by playwright.config.ts):"
          echo "  tests/e2e/__screenshots__/visual.spec.ts/*.png"
          echo ""
          echo "To update baselines, manually trigger visual-modern.yml with updateBaselines=true"
