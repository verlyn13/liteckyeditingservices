# Security Headers for Cloudflare Pages
# https://developers.cloudflare.com/pages/configuration/headers/
#
# NOTE: /admin/* headers are managed by functions/admin/[[path]].ts
# This file only contains headers for general routes and static assets

# Vendored assets (Decap CMS) - immutable caching
/vendor/decap/*
  Cache-Control: public, max-age=31536000, immutable
  X-Content-Type-Options: nosniff

# Default security headers for all other routes
/*
  # HSTS - Force HTTPS for 1 year, include subdomains
  Strict-Transport-Security: max-age=31536000; includeSubDomains; preload

  # Content Security Policy
  # - self for scripts, styles, images
  # - Turnstile CDN for captcha
  # - unsafe-inline for Svelte reactive styles (to be replaced with nonces in future)
  # - data: for inline images and Vite-inlined small scripts
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' data: https://challenges.cloudflare.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' data:; connect-src 'self' https://challenges.cloudflare.com; frame-src https://challenges.cloudflare.com; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests;

  # Prevent clickjacking (allow same-origin iframes for admin preview)
  X-Frame-Options: SAMEORIGIN

  # Restrict browser features
  Permissions-Policy: accelerometer=(), camera=(), geolocation=(), gyroscope=(), magnetometer=(), microphone=(), payment=(), usb=()

  # Already set by Pages, but explicit here for clarity
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin

  # Prevent MIME sniffing in older browsers
  X-Download-Options: noopen
  X-Permitted-Cross-Domain-Policies: none
