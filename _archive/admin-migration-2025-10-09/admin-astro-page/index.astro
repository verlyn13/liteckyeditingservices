---
// Static admin shell page for Decap CMS (self-hosted).
// Vendored bundle loaded from /vendor/decap/ with cache-busting via manifest.
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex" />
    <!-- CSP and Cache-Control set by functions/admin/[[path]].ts (single source of truth) -->
    <!-- Hint Decap where to load the YAML config from (spec-required attributes) -->
    <link href="/admin/config.yml" type="text/yaml" rel="cms-config-url" />
    <title>Content Manager</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  </head>
  <body>
    <!-- Debug OAuth postMessage flow (temporary) -->
    <script is:inline src="/admin/debug.js?v=2025-10-09"></script>
    <!-- Diagnostic script to check CMS state -->
    <script is:inline src="/admin/diagnose.js?v=2025-10-09"></script>
    <!-- Load CMS boot script (no inline JS for hardened CSP) -->
    <!-- Cache-busting version param ensures latest code is loaded -->
    <script is:inline src="/admin/boot.js?v=2025-10-09-spec-aligned" defer></script>
  </body>
  </html>
